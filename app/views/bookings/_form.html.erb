<%= simple_form_for([@boat, @booking]) do |f| %>
    <%= f.input :start_date %>
    <%= f.input :end_date %>
    <%= f.input :total_price %>
    <%= f.input :status %>
    <%= f.button :submit, class: 'btn btn-primary'%>
<% end %>
